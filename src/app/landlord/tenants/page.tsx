export default function TenantsPage() {
    return (
        <div>
            <h1 style={{ fontSize: '2rem', fontWeight: 700, marginBottom: '0.5rem' }}>Tenants</h1>
            <p style={{ color: 'var(--muted-foreground)' }}>Manage your tenants here.</p>
        </div>
    );
}
